#include "main.h"
#include<string.h>
/**
 * _strlen - function that returns length of a string
 * @s:parameter
 * Return: length of string pointed to by s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
